# Security Settings
# Prevent automatic execution of lifecycle scripts from dependencies
# This protects against malicious install/postinstall scripts
ignore-scripts=false

# Use npm registry with strict SSL
registry=https://registry.npmjs.org/
strict-ssl=true

# Enable audit checks
audit=true
audit-level=moderate
